
- parameter names starting with "i" are broken, this is because "inf" is a
  float value and the float test only considers the first character.

- some bug with store_results not in time (cap) (fixed?)
- bug with wildcard probes like wDt(B1*)
- store tran x AND print tran x x crashes

- adms: MIN_NET_NODES is 0.

- some inconsistency about internal device vs internal subckt node in prblist
